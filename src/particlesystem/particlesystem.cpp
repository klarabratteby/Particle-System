#include <particlesystem/particlesystem.h>

/**

update() {
    
    forEach emitter( => e) {
        e->spawnNewParticle(particles&)
    }
    
    forEach effects( => e) {
        e->applyForce(particles&)
    }
    
    forEach particles( => p) {
        p->updatePosition()
    }
    
}
**/
